<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
  xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="
    http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

  <changeSet id="002-create-bill-structures" author="vas-manuel">

    <!-- Agrupadores de cuentas (ej: Gastos Básicos) -->
    <createTable tableName="bill_groups">
      <column name="id" type="UUID">
        <constraints primaryKey="true" nullable="false"/>
      </column>

      <column name="household_id" type="UUID">
        <constraints nullable="false"/>
      </column>

      <column name="name" type="VARCHAR(150)">
        <constraints nullable="false"/>
      </column>

      <column name="created_at" type="TIMESTAMP"/>
      <column name="updated_at" type="TIMESTAMP"/>
    </createTable>

    <addForeignKeyConstraint
      baseTableName="bill_groups"
      baseColumnNames="household_id"
      constraintName="fk_bill_groups_household"
      referencedTableName="households"
      referencedColumnNames="id"/>


    <!-- Cuentas fijas y variables -->
    <createTable tableName="bill_accounts">
      <column name="id" type="UUID">
        <constraints primaryKey="true" nullable="false"/>
      </column>

      <column name="household_id" type="UUID">
        <constraints nullable="false"/>
      </column>

      <column name="bill_group_id" type="UUID"/>

      <column name="name" type="VARCHAR(150)">
        <constraints nullable="false"/>
      </column>

      <!-- FIXED o VARIABLE -->
      <column name="type" type="VARCHAR(20)">
        <constraints nullable="false"/>
      </column>

      <!-- Día del mes (opcional) -->
      <column name="due_day" type="INT"/>

      <!-- Visible en reportes -->
      <column name="visible" type="BOOLEAN">
        <constraints nullable="false"/>
      </column>

      <column name="created_at" type="TIMESTAMP"/>
      <column name="updated_at" type="TIMESTAMP"/>
    </createTable>

    <addForeignKeyConstraint
      baseTableName="bill_accounts"
      baseColumnNames="household_id"
      constraintName="fk_bill_accounts_household"
      referencedTableName="households"
      referencedColumnNames="id"/>

    <addForeignKeyConstraint
      baseTableName="bill_accounts"
      baseColumnNames="bill_group_id"
      constraintName="fk_bill_accounts_group"
      referencedTableName="bill_groups"
      referencedColumnNames="id"/>

  </changeSet>

</databaseChangeLog>
